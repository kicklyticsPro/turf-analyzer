services:
  - type: web
    name: turf-analyzer
    runtime: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: ./startup.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    autoDeploy: true
